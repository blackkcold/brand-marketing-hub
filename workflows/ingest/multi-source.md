# Multi-source Ingestion
Assign every input a FILE id. Resolve duplicate facts by source authority and date, not filename order. Keep conflicting facts visible until resolved. Build one unified content-unit inventory while retaining original file locators. A later web source may verify or update a claim but must not silently overwrite user-provided source content.
