---
name: brand-marketing-hub
description: 品牌营销调研与方案汇总技能（总入口）。当用户提出任何品牌营销类需求时触发，包括"品牌调研"、"IP联名方案"、"IP合作提案"、"IP评估"、"联名报价测算"、"艺人分析报告"、"代言人评估"、"名人备选"、"艺人TGI分析"、"风险预判"、"1+N艺人矩阵"、"传播方案"、"传播规划"、"新品上市传播"、"campaign方案"、"传播作战图"、"RTB构建"、"KOL传播排期"、"md转pptx"、"提案PPT"等。agent 自主识别任务意图，从本skill包中选择对应方法论模块（IP联名/艺人评估/传播方案）与模板完成工作，支持复合任务的多模块串联执行，最终经md→pptx流水线输出pptx初稿。
version: 1.0.0
---

# 品牌营销调研中心（brand-marketing-hub）

本skill是一个自包含的品牌营销工作包：SKILL.md 负责路由决策，references/ 存放三大方法论模块（按需加载），assets/ 集中全部模板、转换脚本与演示样张。

## 第一步：意图识别与路由

判断用户需求属于以下哪类，然后读取对应模块执行：

| 任务信号 | 路由到 | 配套模板 |
|---|---|---|
| IP联名、IP合作提案、IP价值评估、联名权益/报价、Deal Memo、联名礼盒 | references/ip-collab.md | assets/template-ip-collab.md |
| 艺人/代言人/名人评估、TGI分析、舆情风险、1+N矩阵、明星合作玩法 | references/celebrity.md | assets/template-celebrity-report.md |
| 传播方案、campaign、上市传播、RTB、KOL排期、传播作战图、预算分配 | references/campaign.md | assets/template-campaign-plan.md |
| 需要产出pptx（md转pptx、PPT骨架、提案初稿） | 转换规范见本页"输出流水线" | assets/md2pptx.py |

**组合任务**（hub的核心价值）：新品上市全案 = celebrity模块（选人）+ ip-collab模块（IP搭载）+ campaign模块（传播节奏与预算）。按依赖顺序串联：先定人与IP（策略输入），再出传播方案（策略输出），各模块产出独立deck或合并deck。

**意图模糊时先澄清再动手**：问清任务类型（提案/评估/汇报）、目标产品、决策链上的关键问题，不猜。

**趋势类问题**（品牌年轻化、IP营销趋势）：三个模块内均含方法论内核（内容容器四维模型、三阶段演进、双引擎结构），直接引用作答，不必套模板。

## 第二步：执行模块工作流

读取路由到的 reference 文件，按其中的步骤化工作流执行。三个模块共享的方法论内核：
- 内容容器四维模型：产品能力延伸 / 品牌价值观延伸 / TA需求延伸 / 营销节点重构
- 三阶段演进：借IP流量 → 借IP内容 → 共同经营
- 双引擎：广度曝光 + 深度种草，PUSH→PULL
- 三件要事模式：一套内容 + 一个用户事件 + 一个转化载体

## 第三步：输出流水线（md → pptx）

1. 套用 assets/ 对应模板填充为 md 骨架。规范：`# 标题`→封面；`<!-- deck: 副标题 -->`→封面副标题；`## P{n}｜页面标题`→一页slide；`-`/缩进`-`→两级bullet；`> 提示`→演讲者备注。
2. 转换前核查：P编号连续、单页一级要点≤6条、占位符用【】包裹且最终清理、敏感数据脱敏。
3. 执行：`python3 ~/.vbuddy/skills/brand-marketing-hub/assets/md2pptx.py 输入.md 输出.pptx [--theme dark]`（依赖 python-pptx）。
4. 转换后核查：页数与P编号一致、抽查3页标题与备注、文件可打开。
5. 交付时告知：pptx为骨架初稿，视觉（KV/图片/配色）需在PowerPoint中人工调整。

样张参考：assets/samples/ 下有三种模板的转换效果演示。

## 陷阱

- 多模块组合时注意交付物边界：艺人评估报告和传播方案是不同deck，不要混在一套页面里，除非用户明确要求合并。
- 模板中的【】占位符在最终交付前必须全部替换，转换前核查要专门过一遍。
- 表格型内容（报价明细、权益清单）转pptx后是纯文本bullet，需提示用户人工改为表格版式。
- 三个模块的触发词有重叠（如"IP联名传播方案"同时命中ip-collab和campaign），以用户的核心交付物判断主模块：要deck就两个都走，先策略后传播。

## 验证

- 路由决策可复述：能一句话说清"这是X类任务，用了X模块，因为X"。
- 产出的deck通过流水线双重核查（md侧结构+pptx侧读回）。
- 组合任务各模块的产出互相引用一致（同一艺人/IP名称、同一时间线）。
