# OpenAI Presentation Runtime Adapter

Use the host-native presentation capability when available. Inputs are: validated deck_spec, evidence/assets/coverage manifests and the vivo template/reference. Preserve native masters/layouts, charts, tables and editable objects. For edits, inspect the existing presentation first and patch the smallest scope. Always render for QA before delivery.
